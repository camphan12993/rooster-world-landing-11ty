{
  "name": "rooster-world-landing-11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --style=compressed assets/scss/styles.scss assets/css/styles.css",
    "watch:eleventy": "eleventy --serve",
    "watch:sass": "npm run sass -- --watch",
    "dev": "npm-run-all sass --parallel watch:*",
    "build": "npm run sass && eleventy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^0.11.0",
    "bootstrap": "^4.5.0",
    "sass": "^1.26.8"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
